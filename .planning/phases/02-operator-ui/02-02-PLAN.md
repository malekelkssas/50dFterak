---
phase: 02-operator-ui
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/MOBILE/utils/formatters.ts
  - apps/MOBILE/utils/constants/strings.ts
  - apps/MOBILE/components/customers/OrderCard.tsx
autonomous: true
requirements:
  - ORD-03
user_setup: []

must_haves:
  truths:
    - Order rows display monetary values derived only from PlainOrder.snapshotPricePerKg and PlainOrder.snapshotTotal.
    - No UI code path multiplies flourAmount by current global price for display on list/detail surfaces.
  artifacts:
    - Shared format helper(s) use locale `ar-EG` for numeric snapshot display consistent with existing date usage.
  key_links:
    - OrderCard is the single presentation component for orders in UserDetailsScreen and OrdersTab; updating it satisfies both surfaces.
---

<objective>
Wire **order list and detail rows** to show **snapshot money** (**roadmap 02-02**): **`OrderCard`** must present **`snapshotTotal`** and/or **`snapshotPricePerKg`** with consistent **ar-EG** formatting, satisfying **ORD-03**. Legacy pre-snapshot rows remain **0** in data — display as **formatted zero**, not a recomputed total (**02-CONTEXT** D-06).

Purpose: Operators see stable historical totals; changing global price does not change displayed numbers on existing orders.

Output: Formatter helper(s), optional labels in string tables, **OrderCard** UI changes. **OrdersTab** and **UserDetailsScreen** require no separate row logic if **OrderCard** is updated.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-operator-ui/02-CONTEXT.md
@.planning/phases/02-operator-ui/02-UI-SPEC.md
@.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md
@apps/MOBILE/backend/realmHelpers.ts
@apps/MOBILE/components/customers/OrderCard.tsx
@apps/MOBILE/components/customers/OrdersTab.tsx
@apps/MOBILE/utils/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot money formatter</name>
  <files>apps/MOBILE/utils/formatters.ts</files>
  <read_first>apps/MOBILE/utils/formatters.ts, .planning/phases/02-operator-ui/02-UI-SPEC.md</read_first>
  <action>In `apps/MOBILE/utils/formatters.ts`, add a dedicated function, e.g. `formatSnapshotMoney(value: number): string`, that:
- Formats `value` with **`toLocaleString('ar-EG', { minimumFractionDigits: 2, maximumFractionDigits: 2 })`**
- Appends the same currency suffix pattern as `formatCurrency` (literal ` ج.م` after the number) so snapshot totals match existing money style.

Keep existing `formatCurrency` exported; do not break current callers.</action>
<verify>grep formatSnapshotMoney apps/MOBILE/utils/formatters.ts</verify>
<acceptance_criteria>

- `apps/MOBILE/utils/formatters.ts` exports a function named `formatSnapshotMoney`
- Same file contains the literal `minimumFractionDigits` inside or next to the new function
- Same file contains `ar-EG` in the new function’s formatting path
- Same file contains the substring ` ج.م` adjacent to snapshot formatting (same line or within 5 lines of the return)
  </acceptance_criteria>
  <done>Snapshot amounts share a single 2-decimal ar-EG + ج.م formatter.</done>
  </task>

<task type="auto">
  <name>Task 2: Optional labels for rate/total</name>
  <files>apps/MOBILE/utils/constants/strings.ts, apps/MOBILE/utils/constants/index.ts</files>
  <read_first>apps/MOBILE/utils/constants/strings.ts, .planning/phases/02-operator-ui/02-UI-SPEC.md</read_first>
  <action>Add minimal bilingual labels for order row snapshot lines if needed (e.g. ORDER_SNAPSHOT_TOTAL_LABEL, ORDER_SNAPSHOT_RATE_LABEL) under `CUSTOMERS_STRINGS` or a small `ORDER_MONEY_STRINGS` export — **Arabic + English** short labels (e.g. “Total”, “Per kg” / Arabic equivalents). If you can make hierarchy self-evident without labels per UI-SPEC, still add **accessibilityLabel** strings in the next task — those may live inline in **OrderCard** without new constants if preferred; if so, skip new constants and document in acceptance by requiring a11y strings on the money **Text** nodes instead.

**If** you add constants, export from `index.ts`.</action>
<verify>grep -E "ORDER_SNAPSHOT|accessibilityLabel" apps/MOBILE/components/customers/OrderCard.tsx apps/MOBILE/utils/constants/strings.ts || true</verify>
<acceptance_criteria>

- Either `apps/MOBILE/utils/constants/strings.ts` contains new keys whose values include English substrings `Total` **or** `kg` (for rate/total semantics), **or** `OrderCard.tsx` contains `accessibilityLabel={` on a **Text** node that displays snapshot money (grep finds `accessibilityLabel` and `snapshot` case-insensitive in OrderCard)
  </acceptance_criteria>
  <done>Money lines are human- and screen-reader sensible.</done>
  </task>

<task type="auto">
  <name>Task 3: OrderCard snapshot money (ORD-03)</name>
  <files>apps/MOBILE/components/customers/OrderCard.tsx</files>
  <read_first>apps/MOBILE/components/customers/OrderCard.tsx, apps/MOBILE/backend/realmHelpers.ts, .planning/phases/02-operator-ui/02-CONTEXT.md</read_first>
  <action>Update `OrderCard` to show **both**:
- **`formatSnapshotMoney(item.snapshotTotal)`**
- **`formatSnapshotMoney(item.snapshotPricePerKg)`** with a per-kg indicator (reuse `CUSTOMERS_STRINGS.KILO_UNIT` or label constants)

Placement: **secondary** to flour amount + time — trailing column or subline per **02-UI-SPEC** “Order list row” hierarchy; use **Label** (14px) + **Body** (16px) roles via existing **Text** `variant` props.

**Hard rules:**

- **Do not** import `orgSettingsService`, `getRealm`, or read global price for display.
- **Do not** compute display totals as `item.flourAmount * …` anywhere in this file.
- For `snapshotPricePerKg === 0` and `snapshotTotal === 0` (legacy), still show **formatted zeros** (`0,00 ج.م` style via formatter) — **not** em dash — per **02-CONTEXT** D-06.

After edit, run from repo root: `rg "flourAmount\\s*\\*|\\*\\s*.*globalPrice|orgSettingsService" apps/MOBILE/components/customers/OrderCard.tsx` and ensure **no matches**.</action>
<verify>grep -n "snapshotTotal\|snapshotPricePerKg\|formatSnapshotMoney" apps/MOBILE/components/customers/OrderCard.tsx</verify>
<acceptance_criteria>

- `apps/MOBILE/components/customers/OrderCard.tsx` imports `formatSnapshotMoney` from `@mobile/utils/formatters` or `../../utils/formatters` (path must resolve in the app)
- Same file contains `item.snapshotTotal` in JSX or expression used for display
- Same file contains `item.snapshotPricePerKg` in JSX or expression used for display
- Same file does **not** import from `OrgSettingsService` or `../backend/services/OrgSettingsService` (grep exits 1)
- Same file does **not** contain the substring `flourAmount *` (grep exits 1)
  </acceptance_criteria>
  <done>All order rows show snapshot-derived money; legacy rows show honest zeros.</done>
  </task>

</tasks>

<verification>
- [ ] `grep -n "ORD-03" .planning/phases/02-operator-ui/02-02-PLAN.md` shows requirement in frontmatter
- [ ] `rg "OrderCard" apps/MOBILE -l` lists only `UserDetailsScreen.tsx`, `OrdersTab.tsx`, `OrderCard.tsx` — confirm both call sites pick up new UI without further edits
- [ ] `rg "getGlobalPricePerKg|setGlobalPricePerKg" apps/MOBILE/components/customers/OrderCard.tsx` exits 1
</verification>

<success_criteria>

- All tasks completed
- **UserDetailsScreen** and **OrdersTab** lists both reflect changes via **OrderCard**
- Aligns with **02-UI-SPEC** money display and **02-CONTEXT** D-04–D-06

</success_criteria>

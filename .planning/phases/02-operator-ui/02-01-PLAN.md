---
phase: 02-operator-ui
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/MOBILE/utils/constants/navigation.ts
  - apps/MOBILE/utils/constants/strings.ts
  - apps/MOBILE/utils/types/navigation.ts
  - apps/MOBILE/navigation/index.tsx
  - apps/MOBILE/screens/HomeScreen.tsx
  - apps/MOBILE/screens/GlobalFlourPriceScreen.tsx
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
user_setup: []

must_haves:
  truths:
    - Operator opens a dedicated screen, sees the persisted global per-kg price, edits it, and saves without creating an order.
    - All values round-trip through orgSettingsService only (no duplicate source of truth).
  artifacts:
    - New stack screen registered in AppNavigator and RootStackParamList.
    - Bilingual string tables include the exact canonical EN copy from 02-UI-SPEC plus AR counterparts.
  key_links:
    - Primary CTA label is exactly Save flour price (EN constant); save uses orgSettingsService.setGlobalPricePerKg.
---

<objective>
Deliver the **global per-kg flour price** operator surface (**roadmap 02-01**): read/write `OrgSettings.globalPricePerKg` via **`orgSettingsService`**, with validation and copy from **`02-UI-SPEC.md`**, and a **discoverable entry** from **`HomeScreen`**.

Purpose: Satisfy **PRICE-01** (view) and **PRICE-02** (set/update without an order).

Output: Constants, new screen, navigation types, navigator registration, Home affordance.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-operator-ui/02-CONTEXT.md
@.planning/phases/02-operator-ui/02-UI-SPEC.md
@apps/MOBILE/backend/services/OrgSettingsService.ts
@apps/MOBILE/screens/HomeScreen.tsx
@apps/MOBILE/navigation/index.tsx
@apps/MOBILE/utils/types/navigation.ts
@apps/MOBILE/utils/constants/navigation.ts
@apps/MOBILE/components/ui/Text.tsx
@apps/MOBILE/components/ui/TextInput.tsx
@apps/MOBILE/components/ui/Button.tsx
@apps/MOBILE/components/ui/Card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pricing string tables (EN canonical + AR)</name>
  <files>apps/MOBILE/utils/constants/strings.ts, apps/MOBILE/utils/constants/index.ts</files>
  <read_first>apps/MOBILE/utils/constants/strings.ts, .planning/phases/02-operator-ui/02-UI-SPEC.md</read_first>
  <action>Add a new exported constant object (e.g. `PRICING_STRINGS`) in `apps/MOBILE/utils/constants/strings.ts` (or a sibling `strings-pricing.ts` re-exported from `index.ts` if you prefer separation) containing **both** English and Arabic entries for this phase’s pricing UI.

**English keys must use this exact canonical copy** (from 02-UI-SPEC — store as string values, not scattered literals in JSX):

- Primary CTA: `Save flour price`
- Empty heading: `Set your flour price`
- Empty body: `Enter what you charge per kilogram. New orders will use this rate when they are created.`
- Invalid price title: `Enter a valid price per kilogram`
- Invalid price body: `Use a positive number. You can use up to two decimal places.`
- Save failed title: `Could not save price`
- Save failed body: `Check that the app has storage access and try again.`

Add **Arabic** counterparts for every key above (professional operator-facing Arabic; mirror the EN meaning).

Export from `apps/MOBILE/utils/constants/index.ts` alongside existing exports.</action>
<verify>grep -E "PRICING_STRINGS|Save flour price" apps/MOBILE/utils/constants/strings.ts apps/MOBILE/utils/constants/index.ts</verify>
<acceptance_criteria>

- `apps/MOBILE/utils/constants/strings.ts` contains the literal substring `Save flour price`
- Same file contains the literal substring `Set your flour price`
- Same file contains the literal substring `Use a positive number. You can use up to two decimal places.`
- Same file contains the literal substring `Could not save price`
- `apps/MOBILE/utils/constants/index.ts` exports `PRICING_STRINGS` (grep finds `PRICING_STRINGS` on an export line)
  </acceptance_criteria>
  <done>Pricing copy is centralized and matches 02-UI-SPEC English contract with AR pairs.</done>
  </task>

<task type="auto">
  <name>Task 2: GlobalFlourPriceScreen (load, validate, save)</name>
  <files>apps/MOBILE/screens/GlobalFlourPriceScreen.tsx</files>
  <read_first>apps/MOBILE/backend/services/OrgSettingsService.ts, apps/MOBILE/utils/constants/theme.ts, .planning/phases/02-operator-ui/02-UI-SPEC.md, apps/MOBILE/screens/HomeScreen.tsx</read_first>
  <action>Create `apps/MOBILE/screens/GlobalFlourPriceScreen.tsx`:

0. **Header:** Mirror **UserDetailsScreen**’s top bar pattern (`Pressable` + `ChevronLeft` + `navigation.goBack()`, `useColorScheme` / icon color) so the stack screen is dismissible without enabling the native stack header (`headerShown` stays false in `AppNavigator`).

1. On mount, read `orgSettingsService.getGlobalPricePerKg()` and populate a controlled **TextInput** (use `@/components/ui` or `@mobile/components/ui` consistently with **UserDetailsScreen**).
2. Layout follows **02-UI-SPEC**: spacing scale (md/lg), **Heading** ~20px semibold for screen title, body 16px, primary CTA uses **`Button`** with mode/style consistent with **primary** reserved role (save action).
3. When stored global price is **0** and the field is empty / uninitialized, show the **empty state** copy from `PRICING_STRINGS` (heading + body) per UI-SPEC hierarchy (focal: large rate + kg unit + field).
4. **Validation before save:** reject non-positive values and values with more than **two** decimal places; show invalid copy from `PRICING_STRINGS` (title + body) via inline error or dialog/snackbar — must be user-visible.
5. **Save:** `orgSettingsService.setGlobalPricePerKg(parsedNumber)` inside try/catch; on failure show save-failed copy from `PRICING_STRINGS`.
6. **Accessibility:** primary `Button` and `TextInput` have appropriate `accessibilityLabel` or associated labels so the save action is discoverable.

Do **not** import Redux or Realm directly in this screen — service only.</action>
<verify>grep -E "orgSettingsService|getGlobalPricePerKg|setGlobalPricePerKg" apps/MOBILE/screens/GlobalFlourPriceScreen.tsx</verify>
<acceptance_criteria>

- File `apps/MOBILE/screens/GlobalFlourPriceScreen.tsx` exists
- File contains `orgSettingsService`
- File contains `getGlobalPricePerKg`
- File contains `setGlobalPricePerKg`
- File imports `PRICING_STRINGS` from `@mobile/utils/constants` or `@/utils/constants`
- File contains `goBack` (caller on navigation for back affordance)
- File does **not** contain the substring `getRealm(` (grep exits 1)
  </acceptance_criteria>
  <done>Pricing screen reads/writes global price through OrgSettingsService with spec validation and copy.</done>
  </task>

<task type="auto">
  <name>Task 3: Register stack screen + Home entry</name>
  <files>apps/MOBILE/utils/constants/navigation.ts, apps/MOBILE/utils/types/navigation.ts, apps/MOBILE/navigation/index.tsx, apps/MOBILE/screens/HomeScreen.tsx</files>
  <read_first>apps/MOBILE/utils/types/navigation.ts, apps/MOBILE/navigation/index.tsx, apps/MOBILE/screens/HomeScreen.tsx</read_first>
  <action>
1. Add `GLOBAL_FLOUR_PRICE: 'GlobalFlourPrice'` (or equivalent string) to `SCREENS` in `apps/MOBILE/utils/constants/navigation.ts`.
2. Extend `RootStackParamList` in `apps/MOBILE/utils/types/navigation.ts` with `GlobalFlourPrice: undefined` (use the exact screen name string you register).
3. In `apps/MOBILE/navigation/index.tsx`, import `GlobalFlourPriceScreen` and add `<Stack.Screen name="GlobalFlourPrice" component={GlobalFlourPriceScreen} />` (name must match `RootStackParamList` key).
4. On `HomeScreen`, add a tappable control ( **Button** or **Pressable** inside the existing **Card**) that navigates to the new stack screen the same way **CustomersTab** reaches **UserDetails**: `useNavigation<AppNavigationProp<'MainTabs'>>()` and `navigation.navigate(SCREENS.GLOBAL_FLOUR_PRICE)` (add `GLOBAL_FLOUR_PRICE` to `SCREENS` first). If TypeScript rejects the route name, fix typings in `utils/types/navigation.ts` using `CompositeNavigationProp` (tab + parent stack) until `pnpm exec tsc --noEmit` passes. Minimum **48×48** touch target for the control per 02-UI-SPEC.
5. Set `accessibilityLabel` on the Home entry (English or bilingual is fine; must mention flour price / pricing intent).</action>
<verify>grep GlobalFlourPrice apps/MOBILE/navigation/index.tsx apps/MOBILE/utils/types/navigation.ts</verify>
<acceptance_criteria>
- `apps/MOBILE/utils/constants/navigation.ts` contains `GLOBAL_FLOUR_PRICE` and the literal `GlobalFlourPrice`
- `apps/MOBILE/utils/types/navigation.ts` contains `GlobalFlourPrice` as a key in `RootStackParamList`
- `apps/MOBILE/navigation/index.tsx` contains `GlobalFlourPrice` in a `Stack.Screen` `name` prop
- `apps/MOBILE/screens/HomeScreen.tsx` contains `navigate` and the substring `GlobalFlourPrice`
- `pnpm exec tsc --noEmit` run from `apps/MOBILE` exits 0 (or project’s documented typecheck passes)
</acceptance_criteria>
<done>Pricing screen is reachable from Home and registered in the root stack.</done>
</task>

</tasks>

<verification>
- [ ] `grep -n "PRICE-01\\|PRICE-02" .planning/phases/02-operator-ui/02-01-PLAN.md` shows requirements in frontmatter
- [ ] `grep -R "Save flour price" apps/MOBILE/utils/constants/` returns matches
- [ ] No new order-creation call introduced on the pricing screen (`grep -n "addOrder" apps/MOBILE/screens/GlobalFlourPriceScreen.tsx` returns nothing)
</verification>

<success_criteria>

- All tasks completed
- Implements **02-CONTEXT** D-01, D-02, D-07 and **02-UI-SPEC** copy/spacing/color roles
- No unrelated refactors

</success_criteria>

---
phase: 01-pricing-snapshots-data-layer
plan: '03'
type: execute
wave: 3
depends_on:
  - '01-02'
files_modified:
  - .planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md
autonomous: true
requirements:
  - ORD-04
  - PRICE-03
  - ORD-01
  - ORD-02
user_setup: []

must_haves:
  truths:
    - ORD-04 behavior (legacy orders show 0 for snapshot-derived money until product changes) is documented in-repo for Phase 2+ readers.
    - Manual verification steps confirm a new order has non-zero snapshots when global price &gt; 0 and that changing global price leaves old order snapshots unchanged.
  artifacts:
    - File `.planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md` exists with explicit bullets for legacy vs new orders.
  key_links:
    - Document references `01-CONTEXT.md` decision D-05 and REQUIREMENTS ORD-04 wording.
---

<objective>
**Document and verify** ORD-04 (legacy order rule) and end-to-end snapshot behavior: add `01-LEGACY-ORDERS.md`, and provide a **manual verification checklist** (no Jest in repo) that an executor can run on a dev build.

Purpose: Close ORD-04 explicitly; regression-proof PRICE-03 / ORD-01 / ORD-02 for human QA.

Output: Legacy policy doc + checklist section in this plan executed as typed steps in SUMMARY.md during execute-phase.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md
@apps/MOBILE/backend/services/OrderService.ts
@apps/MOBILE/backend/services/OrgSettingsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ORD-04 legacy policy doc</name>
  <files>.planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md</files>
  <read_first>.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `.planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md` containing:

1. Title `# Phase 1 — Legacy orders (ORD-04)`

2. A section `## Requirement` quoting that **ORD-04** requires explicit documented behavior (paraphrase 1–2 sentences from REQUIREMENTS.md).

3. A section `## Decision (D-05)` stating: orders created **before** schema v4 have `snapshotPricePerKg` and `snapshotTotal` **0** via schema defaults/migration; Phase 2 UI will show **0** for money derived from snapshots unless product changes later; **accepted** per `01-CONTEXT.md`.

4. A section `## Verification` with 3+ numbered manual steps:
   - Open app with migrated DB, confirm app launches without Realm migration crash
   - Set global price &gt; 0 via **OrgSettingsService** (or temporary dev hook if executor adds a one-line dev-only call — prefer documenting `setGlobalPricePerKg` from a RN debug screen only if one exists; otherwise state "use React Native debugger / temporary button" as optional executor note)
   - Create new order, inspect Realm (or log `toPlainOrder`) to confirm snapshots match global price and rounded total
   - Change global price, confirm **previous** order's `snapshotPricePerKg` and `snapshotTotal` unchanged

Use the exact requirement id **ORD-04** and decision id **D-05** as literal strings in the doc.</action>
<verify>test -f .planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md</verify>
<acceptance_criteria> - File `.planning/phases/01-pricing-snapshots-data-layer/01-LEGACY-ORDERS.md` exists - File contains literal `ORD-04` - File contains literal `D-05` - File contains literal `snapshotPricePerKg` - File contains a Markdown heading line starting with `## Verification`
</acceptance_criteria>
<done>Legacy policy and manual steps are documented for operators and Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Codebase consistency audit (grep-only)</name>
  <files>apps/MOBILE/backend/services/OrderService.ts, apps/MOBILE/backend/realm.ts</files>
  <read_first>apps/MOBILE/backend/services/OrderService.ts, apps/MOBILE/backend/realm.ts, apps/MOBILE/backend/models/Order.ts</read_first>
  <action>Run (or document results of) static greps — **no behavior change** unless a bug is found:

1. `rg "schemaVersion" apps/MOBILE/backend/realm.ts` must show version **4**
2. `rg "snapshotPricePerKg|snapshotTotal" apps/MOBILE/backend` — every **assignment** outside `addOrder`'s `realm.create` should be absent (only property reads or create object literal)

If an unexpected assignment exists, fix it in this task and note deviation in execute-phase SUMMARY.

If clean, add one comment line in `01-LEGACY-ORDERS.md` under `## Verification` subheading `### Grep audit` listing the exact `rg` commands used and result "PASS".</action>
<verify>rg "schemaVersion: 4" apps/MOBILE/backend/realm.ts</verify>
<acceptance_criteria> - `01-LEGACY-ORDERS.md` contains the substring `Grep audit` OR `rg ` - `apps/MOBILE/backend/realm.ts` still contains `schemaVersion: 4` after edits
</acceptance_criteria>
<done>Audit confirms no stray snapshot mutations; doc records grep audit.</done>
</task>

</tasks>

<verification>
- [ ] `01-LEGACY-ORDERS.md` exists and is internally consistent with `01-CONTEXT.md` D-05
- [ ] Manual checklist in doc references both legacy (0) and new-order (non-zero) cases
</verification>

<success_criteria>

- ORD-04 explicitly documented; no silent wrong totals claim without migration path
- Cross-check PRICE-03 / ORD-01 / ORD-02 via manual steps for regressions

</success_criteria>

---
phase: 01-pricing-snapshots-data-layer
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/MOBILE/backend/models/OrgSettings.ts
  - apps/MOBILE/backend/models/Order.ts
  - apps/MOBILE/backend/realm.ts
autonomous: true
requirements:
  - PRICE-03
  - ORD-01
  - ORD-02
  - ORD-04
user_setup: []

must_haves:
  truths:
    - Realm opens at schemaVersion 4 with OrgSettings and new Order snapshot fields present in the schema.
    - Existing DBs migrate without runtime error; pre-existing Order rows receive snapshot doubles of 0 (Realm defaults / migration).
  artifacts:
    - apps/MOBILE/backend/models/OrgSettings.ts exists and exports a Realm.Object class with schema name OrgSettings.
    - apps/MOBILE/backend/realm.ts registers OrgSettings and bumps schemaVersion from 3 to 4.
  key_links:
    - Order model includes two new non-optional double properties intended for create-time snapshots (names chosen in this plan).
---

<objective>
Add **OrgSettings** (singleton global price) and **Order** snapshot fields (`snapshotPricePerKg`, `snapshotTotal` as `double`), bump **schemaVersion** to **4**, and add a **migration** that ensures one `OrgSettings` row exists with `globalPricePerKg === 0` when upgrading from v3.

Purpose: Persistence shape for Phase 1 pricing and immutable per-order money snapshots (ORD-01, ORD-02, ORD-04 defaults).

Output: New model file, updated `Order` schema, updated `realm.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md
@apps/MOBILE/backend/realm.ts
@apps/MOBILE/backend/models/Order.ts
@apps/MOBILE/backend/models/User.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OrgSettings Realm model</name>
  <files>apps/MOBILE/backend/models/OrgSettings.ts</files>
  <read_first>apps/MOBILE/backend/models/Order.ts, apps/MOBILE/backend/models/User.ts, apps/MOBILE/backend/realm.ts</read_first>
  <action>Create `apps/MOBILE/backend/models/OrgSettings.ts` exporting `OrgSettings` extending `Realm.Object` with:
- `static schema.name`: exactly `'OrgSettings'`
- `primaryKey`: `'_id'` with type `string` (not objectId)
- Properties: `_id` (string), `globalPricePerKg` (double)
- Use the same import style as `Order.ts` (`Realm`, `ObjectSchema` pattern)

Use **fixed singleton id**: store documents with `_id === 'singleton'` only (document this in a one-line comment on the class).

Default for new objects: `globalPricePerKg` defaults to `0` via Realm schema `default` on the property if supported; otherwise migration will set it.</action>
<verify>grep OrgSettings apps/MOBILE/backend/models/OrgSettings.ts && grep "name: 'OrgSettings'" apps/MOBILE/backend/models/OrgSettings.ts</verify>
<acceptance_criteria> - File `apps/MOBILE/backend/models/OrgSettings.ts` exists - File contains `name: 'OrgSettings'` - File contains `primaryKey: '_id'` - File contains `globalPricePerKg` - File contains the literal substring `singleton`
</acceptance_criteria>
<done>OrgSettings model file created with schema as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Order schema with snapshot doubles</name>
  <files>apps/MOBILE/backend/models/Order.ts</files>
  <read_first>apps/MOBILE/backend/models/Order.ts, apps/MOBILE/backend/realm.ts</read_first>
  <action>In `Order.static.schema.properties`, add two required `double` properties with Realm defaults `0`:
- `snapshotPricePerKg` — per-kg price frozen at order creation (ORD-01)
- `snapshotTotal` — `flourAmount × snapshotPricePerKg` at creation, rounded per plan 01-02 (ORD-02); default `0` satisfies ORD-04 for legacy rows

Do not remove or rename existing properties.</action>
<verify>grep -E "snapshotPricePerKg|snapshotTotal" apps/MOBILE/backend/models/Order.ts</verify>
<acceptance_criteria> - `apps/MOBILE/backend/models/Order.ts` contains the exact property key `snapshotPricePerKg` - Same file contains the exact property key `snapshotTotal` - Both are typed as `'double'` in the schema object (grep shows `double` on same logical line or adjacent lines for each key in the properties block)
</acceptance_criteria>
<done>Order schema includes snapshot fields with double type and default 0.</done>
</task>

<task type="auto">
  <name>Task 3: Register schema and migration (v3 → v4)</name>
  <files>apps/MOBILE/backend/realm.ts</files>
  <read_first>apps/MOBILE/backend/realm.ts, apps/MOBILE/backend/models/OrgSettings.ts, apps/MOBILE/backend/models/Order.ts</read_first>
  <action>Update `apps/MOBILE/backend/realm.ts`:
1. Import `OrgSettings` from `./models/OrgSettings`
2. Add `OrgSettings` to the `schema` array: `[Invoice, User, Order, OrgSettings]` (keep existing order except insert OrgSettings)
3. Set `schemaVersion` from `3` to `4`
4. Add `onMigration(oldRealm, newRealm)` that runs only when `oldRealm.schemaVersion < 4`:
   - If `newRealm.objects('OrgSettings').length === 0`, `newRealm.create('OrgSettings', { _id: 'singleton', globalPricePerKg: 0 })`
   - Do **not** loop orders to set snapshot fields — Realm defaults on new properties cover ORD-04 (`0` for legacy)

Use Realm JS migration API consistent with realm ^12.x (same project version).</action>
<verify>grep schemaVersion apps/MOBILE/backend/realm.ts</verify>
<acceptance_criteria> - `apps/MOBILE/backend/realm.ts` contains `schemaVersion: 4` (digit 4 after schemaVersion) - Same file imports `OrgSettings` from `./models/OrgSettings` or `./models/OrgSettings.ts` path as used elsewhere in the project - Same file includes `OrgSettings` in the `schema:` array - Same file contains `onMigration` and the string `OrgSettings` inside the migration callback body - Same file contains the literal `'singleton'` inside the migration callback body
</acceptance_criteria>
<done>Realm opens at v4 with OrgSettings registered and singleton row created on upgrade.</done>
</task>

</tasks>

<verification>
- [ ] `grep -n "schemaVersion" apps/MOBILE/backend/realm.ts` shows `4`
- [ ] `grep -n "snapshotPricePerKg\\|snapshotTotal" apps/MOBILE/backend/models/Order.ts` returns matches
- [ ] App / Metro bundler compiles with no TypeScript errors in touched files (run project-appropriate check if available)
</verification>

<success_criteria>

- All tasks completed
- New schema matches `.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md` decisions D-01, D-03, D-04, D-05 (singleton settings, double snapshots, legacy 0)
- No unrelated refactors

</success_criteria>

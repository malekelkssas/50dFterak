---
phase: 01-pricing-snapshots-data-layer
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/MOBILE/backend/services/OrgSettingsService.ts
  - apps/MOBILE/backend/services/OrderService.ts
  - apps/MOBILE/backend/realmHelpers.ts
autonomous: true
requirements:
  - PRICE-03
  - ORD-01
  - ORD-02
user_setup: []

must_haves:
  truths:
    - Reading/updating global price goes through OrgSettings singleton `_id === 'singleton'` without creating duplicate rows.
    - New orders persist `snapshotPricePerKg` from current `globalPricePerKg` and `snapshotTotal` as round(flourAmount * price, 2 half-up).
    - Mutating global price does not change `snapshotPricePerKg` or `snapshotTotal` on existing Order objects.
  artifacts:
    - OrgSettingsService (or equivalently named service) with get/set for global price.
    - OrderService.addOrder writes both snapshot fields; other write paths do not clear snapshots incorrectly.
  key_links:
    - `PlainOrder` and `toPlainOrder` expose snapshot fields for Redux/UI consumers in later phases.
---

<objective>
Implement **read/update global per-kg price** via **OrgSettings** singleton, **write snapshots on create** in `OrderService.addOrder` using **half-up rounding to 2 decimal places** for `snapshotTotal`, extend **plain** types/helpers, and add **low-cost guards** so snapshot fields are not overwritten after creation.

Purpose: Satisfy PRICE-03, ORD-01, ORD-02 at the service layer.

Output: New service file, updated `OrderService.ts`, updated `realmHelpers.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-pricing-snapshots-data-layer/01-CONTEXT.md
@apps/MOBILE/backend/realm.ts
@apps/MOBILE/backend/models/OrgSettings.ts
@apps/MOBILE/backend/models/Order.ts
@apps/MOBILE/backend/services/OrderService.ts
@apps/MOBILE/backend/realmHelpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrgSettingsService for global price</name>
  <files>apps/MOBILE/backend/services/OrgSettingsService.ts</files>
  <read_first>apps/MOBILE/backend/services/OrderService.ts, apps/MOBILE/backend/realm.ts, apps/MOBILE/backend/models/OrgSettings.ts</read_first>
  <action>Create `apps/MOBILE/backend/services/OrgSettingsService.ts` mirroring **singleton pattern** of `OrderService`:
- `getInstance()` private constructor
- `getGlobalPricePerKg(): number` — `realm.objectForPrimaryKey('OrgSettings', 'singleton')` if null then **create** `{ _id: 'singleton', globalPricePerKg: 0 }` inside `realm.write` and return `0`
- `setGlobalPricePerKg(value: number): void` — ensure row exists, then `realm.write` set `globalPricePerKg` to `value`

Export a module singleton `orgSettingsService` analogous to `orderService` at bottom of `OrderService.ts`.

Do not import UI layers.</action>
<verify>grep -E "getGlobalPricePerKg|setGlobalPricePerKg" apps/MOBILE/backend/services/OrgSettingsService.ts</verify>
<acceptance_criteria> - File `apps/MOBILE/backend/services/OrgSettingsService.ts` exists - File contains `getGlobalPricePerKg` - File contains `setGlobalPricePerKg` - File contains the literal `'singleton'` at least once - File exports `orgSettingsService` (grep matches this exact export name)
</acceptance_criteria>
<done>Service can read and write global price on the singleton row.</done>
</task>

<task type="auto">
  <name>Task 2: addOrder writes snapshots with rounding rule</name>
  <files>apps/MOBILE/backend/services/OrderService.ts</files>
  <read_first>apps/MOBILE/backend/services/OrderService.ts, apps/MOBILE/backend/services/OrgSettingsService.ts, apps/MOBILE/backend/models/Order.ts</read_first>
  <action>In `OrderService.addOrder`, before `realm.create(Order, {...})`:
1. Call `orgSettingsService.getGlobalPricePerKg()` to get `pricePerKg` (number).
2. Compute raw total: `data.flourAmount * pricePerKg`
3. Compute `snapshotTotal` = `Math.round(rawTotal * 100) / 100` (half-up to 2 decimals; document in a single-line comment on the line above)
4. Pass into `realm.create`: `snapshotPricePerKg: pricePerKg`, `snapshotTotal`

Ensure **toggleDone** and **deleteOrder** never assign to `snapshotPricePerKg` or `snapshotTotal` (they should already not; verify and add no new writes to those fields).</action>
<verify>grep snapshotPricePerKg apps/MOBILE/backend/services/OrderService.ts</verify>
<acceptance_criteria> - `apps/MOBILE/backend/services/OrderService.ts` imports `orgSettingsService` from `./OrgSettingsService` or `./OrgSettingsService.ts` (match project import style) - `realm.create(Order,` block includes the keys `snapshotPricePerKg` and `snapshotTotal` as object properties - File contains `Math.round` and `100` on the same line or adjacent lines for the rounding rule (grep `Math.round` in file)
</acceptance_criteria>
<done>New orders persist snapshots from current global price and rounded total.</done>
</task>

<task type="auto">
  <name>Task 3: PlainOrder + toPlainOrder + snapshot immutability guard</name>
  <files>apps/MOBILE/backend/realmHelpers.ts, apps/MOBILE/backend/services/OrderService.ts</files>
  <read_first>apps/MOBILE/backend/realmHelpers.ts, apps/MOBILE/backend/models/Order.ts, apps/MOBILE/backend/services/OrderService.ts</read_first>
  <action>1. Add to `PlainOrder` type: `snapshotPricePerKg: number` and `snapshotTotal: number` (both required, not optional).

2. In `toPlainOrder`, copy `order.snapshotPricePerKg` and `order.snapshotTotal` into the returned object.

3. Add a **guard** in `OrderService`: private method `assertSnapshotsUnchanged(order: Order, priorSnapshotPrice: number, priorSnapshotTotal: number)` that throws `Error` if `order.snapshotPricePerKg !== priorSnapshotPrice || order.snapshotTotal !== priorSnapshotTotal`.

4. Call this guard at the **end** of the `realm.write` callback in `toggleDone` (capture `priorSnapshotPrice`/`priorSnapshotTotal` before write, compare after mutations to `doneAt`/`user` only).

5. Call the same guard at end of `deleteOrder`'s write **before** delete — if snapshots changed, throw (should never happen; guard documents PRICE-03).</action>
   <verify>grep snapshotPricePerKg apps/MOBILE/backend/realmHelpers.ts</verify>
   <acceptance_criteria> - `apps/MOBILE/backend/realmHelpers.ts` type `PlainOrder` includes the substring `snapshotPricePerKg` and `snapshotTotal` - `toPlainOrder` assigns both fields from `order` - `apps/MOBILE/backend/services/OrderService.ts` contains `assertSnapshotsUnchanged` as a substring - `toggleDone` method body contains `assertSnapshotsUnchanged` (grep -n in file: line number for `assertSnapshotsUnchanged` is greater than line for `toggleDone(` and less than line for `deleteOrder(`)
   - `deleteOrder` method body contains `assertSnapshotsUnchanged` (line number for call is greater than line for `deleteOrder(`)
     </acceptance_criteria>
     <done>Plain objects expose snapshots; guards detect accidental snapshot mutation on existing orders.</done>
     </task>

</tasks>

<verification>
- [ ] Grep shows `snapshotPricePerKg` in `OrderService.ts`, `realmHelpers.ts`, `Order.ts`
- [ ] No code path sets `snapshotPricePerKg` or `snapshotTotal` outside `addOrder` create (grep `snapshotPricePerKg\\s*=` in `apps/MOBILE/backend` — should only hit create payload or model defaults, not reassignment in toggleDone/deleteOrder)
</verification>

<success_criteria>

- PRICE-03: updating global price cannot change existing order snapshots (guards + no writes)
- ORD-01 / ORD-02: new orders store per-kg snapshot and derived total with documented rounding
- Matches `01-CONTEXT.md` D-03, D-04 numeric type double

</success_criteria>
